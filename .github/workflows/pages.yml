name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Låt bara en deploy köra i taget, avbryt inte pågående körning
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      # Kopiera sajten till _site, utan node_modules och repo-metadata
      - name: Bygg _site
        run: |
          mkdir _site
          rsync -a --exclude=.git --exclude=.github --exclude=_site \
                --exclude=node_modules --exclude=CLAUDE.md \
                ./ _site/

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
